{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna exec --stream -- babel src --out-dir dist",
    "test": "jest",
    "prerelease": "npm run build",
    "release": "lerna publish --contents dist"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
