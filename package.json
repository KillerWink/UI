{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna exec --scope @killerui/web --stream -- babel src --out-dir dist",
    "test": "jest",
    "prerelease": "npm run build",
    "release": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
