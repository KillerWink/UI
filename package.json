{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna exec --scope web --stream -- babel src --out-dir dist",
    "test": "jest",
    "release": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
